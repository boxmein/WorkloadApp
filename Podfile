project 'Workload.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Workload' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :ios, '12.0'
  # Pods for Workload
  pod 'Alamofire', '~> 4.7'


  target 'WorkloadTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WorkloadUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
